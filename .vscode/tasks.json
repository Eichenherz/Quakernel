{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build HDD",
      "type": "shell",
      "command": "make",
      "args": [
        "build-hdd",
        "TOOLCHAIN=${input:toolchain}",
        "ARCH=${input:arch}",
        "BUILD=${input:build}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always"
      },
      "problemMatcher": []
    }
  ],
  "inputs": [
    {
      "id": "toolchain",
      "type": "pickString",
      "description": "Select Toolchain",
      "options": ["llvm", "gcc"],
      "default": "llvm"
    },
    {
      "id": "arch",
      "type": "pickString",
      "description": "Select Architecture",
      "options": ["x86_64", "aarch64", "riscv64", "loongarch64"],
      "default": "x86_64"
    },
    {
      "id": "build",
      "type": "pickString",
      "description": "Select Build Mode",
      "options": ["Release", "ReleaseWithDebugInfo", "Debug"],
      "default": "ReleaseWithDebugInfo"
    }
  ]
}
